"""Sample LangGraph apps showcasing how to plug graphs into agentseek-api.

Each subpackage exposes a compiled ``graph`` that the API registers under a
matching ``graph_id`` (see ``src/agentseek_api/services/sample_graphs.py``).
Copy any of these subdirectories as a starter template for your own app.
"""
